<header class="nav">
  <div class="container">
    <div class="col-md-12">
      <ul class="nav nav-tabs nav-span">
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "About", about_path %></li>
          <% if current_user %>
            <li><%= link_to "Currently signed in as #{current_user.name}", user_path(current_user) %></li>
            <li><%= link_to "Sign Out", session_path(current_user), method: :delete %></li>
          <% else %>
            <li><%= link_to "Sign In", new_session_path %></li>
            <li><%= link_to "Sign Up", new_user_path %></li>
          <% end %>
        </ul>
    </nav>
  </div>
</header>

<h1>Application Details</h1>
<p><strong>Name:</strong>  <%= @application.name %></p></br>
<p><strong>URL:</strong>  <%= @application.url %></p>
<%= link_to "Remove From List", [@user, @application], method: :delete, class: 'btn btn-danger', data: { confirm: "Are you sure?" }%>
<%= link_to "Back", user_path(@user), class: 'btn btn-warning' %>
